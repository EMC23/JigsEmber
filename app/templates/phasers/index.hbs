<div class="wrapper">
    <div class="container article">
        <h2>Phasers 9</h2>
        <ul class="grid">
            {{#each node--phaser as |phaser|}}
                x <h1>{{phaser.title}}</h1>
            {{/each}}
        </ul>
    </div>
</div>
{{outlet}}
